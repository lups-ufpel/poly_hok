# PolyHok - Core

This is the core module of the PolyHok project, which provides a set of macros for defining PolyHok modules, functions for creating and managing GPU Nx (GNx) structures, spawning kernels, and type inference. It also defines the `BackendBehavior` that all backends must implement.
